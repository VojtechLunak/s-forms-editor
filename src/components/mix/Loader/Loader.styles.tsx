import { makeStyles } from '@mui/material/styles';

export default makeStyles(() => ({
  loader: {
    margin: 0,
    position: 'absolute',
    top: '50%',
    left: '50%',
    height: '70px',
    marginLeft: '-35px',
    marginTop: '-35px'
  }
}));
